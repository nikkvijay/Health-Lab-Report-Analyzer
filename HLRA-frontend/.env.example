# API Configuration
VITE_API_URL=http://localhost:8000

# App Configuration
VITE_APP_NAME="Health Report Analyzer"
VITE_APP_VERSION="1.0.0"

# Public App URL (for shared links) - OPTIONAL
# This is the URL where users will access shared reports
# If not set, it will automatically use the current browser URL
# Only set this if your shared links need a different domain than where the app is accessed
# VITE_PUBLIC_APP_URL=https://your-app-domain.com